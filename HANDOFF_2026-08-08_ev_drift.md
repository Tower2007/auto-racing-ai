# 引継ぎ資料 (2026-08-08, 出先 PC → 家 PC)

出先 PC には `data/` 配下(`odds_ts` / `odds_snapshots.csv` 等)が無いため、
**測定スクリプトだけ書いて実行は家 PC に委ねる**。本書はその実行手順と、
出た数字をどう解釈して何を変えるかの判断基準。

- ブランチ: `main`(push 済み)
- 関連コミット:
  ```
  fe8d9c5 feat(analysis): odds_ts から EV drift カーブを測る ev_drift_curve.py
  ea32002 docs: 自動投票の稼働状況を実態に合わせて修正 + 事故ファイル nul 削除
  ```

---

## 1. 何が問題か

2026-08-07 の夜次反省ログに 1 行:

> 発火EV→確定EV で閾値 1.5 を跨いだレースが 4/6 件

発走 4 分前に `ev_avg_calib >= 1.50` と判断して発注した pick の **2/3 が、
確定オッズでは EV < 1.50 に落ちていた**。つまり実質は期待値の無い賭けを
打っている疑いがある。

これは新しい問題ではなく、**3 ヶ月間まったく改善していない**:

| 時点 | EV 維持率 | 出典 |
|---|---:|---|
| 2026-05-04 | **30.8%** | `docs/ev_strategy_findings.md` の既存表(n=16) |
| 2026-08-07 | **33%** (4/6) | `logs/reflections/2026-08.md` |

この間 `LEAD_MIN` を 5 → 2 → 4 分と動かしているのに水準が変わっていない。
**LEAD_MIN の調整では解けない、系統的なバイアス**と見るべき段階。
累積 ROI 88.3% (確定 274R) が 100% を割っているのとも整合する。

### なぜ今なら測れるのか

`data/odds_ts/*.jsonl`(2026-07-05〜、`AutoraceOddsTsCollector` が収集)に
発走 **-60/-30/-15/-8/-4/-3/+2 分の 7 時点**スナップショットが約 1 ヶ月分ある。
従来の `ev_persistence_by_leadmin.py` は「発火 vs 確定」の 2 点比較だったが、
これで **drift カーブ全体**が引けて、掛けるべき割引係数まで出せる。

---

## 2. 家 PC でやること

```bash
git pull origin main
python scripts/ev_drift_curve.py --thr 1.50 --snapshots
```

`--snapshots` は `data/odds_snapshots.csv` を結合して「実際に発注した pick の
閾値跨ぎ件数」を実測するオプション(出先 PC では動かせなかった部分)。
重い場合は `--since 2026-07-20` で期間を絞れる。

事前に計算ロジックだけ確認したいなら:

```bash
python scripts/ev_drift_curve.py --self-test
```

合成データで ratio 1.5 / 0.5 と異常オッズ除外を検算する(出先 PC で PASS 済)。

### 原理(結合が要らない理由)

```
EV(t) = pred_calib × (fns_min(t) + fns_max(t)) / 2
```

`pred_calib` は同一レース・同一車なら時点によらず一定。よって

```
EV(t) / EV(+2) = odds_avg(t) / odds_avg(+2)
```

となり、**モデル出力を結合しなくても複勝オッズ比だけで EV 比率が出る**。
`--snapshots` は閾値跨ぎの「実件数」を数えるためだけに使う。

---

## 3. 出力の読み方

```
    offset      n  odds_avg  ratio_mu  ratio_med    >1.0率
       -4m    576     7.078    1.0300     1.0200   100.0%
```

- `ratio = odds_avg(t) / odds_avg(+2min)` = `EV(t) / EV(確定)`
- **ratio > 1** → 発火時のほうがオッズが高い = **EV を過大評価していた**
  (締切直前に金が集まってオッズが下がる = 本命化)
- **ratio < 1** → 発火時のほうが低い = EV を過小評価(穴化)

`[全車]` と `[最人気車のみ]` の 2 本が出る。**実際に発注するのは pred-top1
= 概ね人気車**なので、判断は原則 `[最人気車のみ]` を見る。

---

## 4. 判断基準(数字が出てからブレないよう先に決めておく)

| `>1.0率` (最人気車 -4min) | 解釈 | やること |
|---|---|---|
| 〜55% | drift はほぼランダム | **補正しない**。ROI 88.3% の原因は別(控除率・校正・selection bias)を疑う |
| 55〜65% | 弱い系統性 | 保留。もう 1 ヶ月 odds_ts を貯めて再測定 |
| **65% 以上** かつ `ratio_med >= 1.03` | **系統的バイアス確定** | 下記のどちらかを実装 |

n の下限: 最人気車の -4min ペアが **n < 200 なら判断保留**(ノイズ規律)。
7/5〜8/7 で約 1 ヶ月あるので、欠測を引いても足りるはず。足りなければ
`--since` を外して全期間で測る。

### 実装するなら 2 択

**(A) 発火時 EV に割引係数を掛ける ← 推奨**

`daily_predict.py` の EV 算出箇所(`ev_raw = feat["pred_calib"] * (odds_min + odds_max) / 2`
の直後、`ev_avg_calib` を作るところ)に定数倍を入れる。

- 利点: EV の絶対値が意味を持つ **三連系 (thr=1.80) の判定にも一貫して効く**。
  メール・`rt3_paper.csv` などに残る EV も実態に近い値になる
- 係数は `[最人気車のみ]` の `1/ratio_med` を使う

**(B) 発火時 thr を実効値まで引き上げる**

- 利点: 実装 1 行
- 欠点: **EV 値そのものは過大表示のまま**。メールにも paper CSV にも
  歪んだ EV が記録され続けるので、後日の検証がやりにくい

---

## 5. 落とし穴・先に潰しておいた点

- ⚠️ **発注数が減る**。thr=1.50 の実効が 1.60 になれば pick は減る。
  `reports/ev_threshold_sweep_2026-05-31.md` では thr=1.50 で 〜2.5 本/日、
  thr=2.00 で 〜1 本/日。**補正後にどれだけ減るかを先に見積もること**
  (`scripts/ev_threshold_sweep.py` の thr を実効値にして再スイープするのが早い)。
  賭け機会が消えすぎるなら、そもそも thr 1.50 の選択理由(総利益優先)を
  再検討する話になる
- ⚠️ **全車と最人気車で係数が大きく違う可能性**。人気車ほど本命化が強いはず。
  差が 0.03 以上あるなら、補正は必ず **pick 集団(最人気車)側**の係数を使う
- ⚠️ ratio は「賭けた pick」ではなく全車で測っている。`fav_rank==1`(確定
  複勝オッズ最小)は pred-top1 の**代理**であって同一ではない。`--snapshots`
  を付けると実 pick での跨ぎ率が出るので、代理がどれだけ妥当かはそこで確認できる
- 異常オッズ判定は `daily_predict` と同一基準(`max>50` / `max/min>20` /
  `min=max<1.1`)を再利用済。EV 59.28 バグと同じ罠は踏まない
- `fav_rank` は確定オッズのレース単位順位で決定。offset ごとの欠測で
  「最人気車」の定義がブレないようにしてある
- スクリプトはダミー jsonl でフルパス実行を検証済(仕込み値 1.0840 に対し
  実測 1.0833)。CLAUDE.md「数値判断のチェックポイント」に従い、
  **結果を信じる前にまず `--self-test` を通すこと**

---

## 6. 関連ファイル

| ファイル | 役割 |
|---|---|
| `scripts/ev_drift_curve.py` | 本件の測定スクリプト(新規) |
| `scripts/ev_persistence_by_leadmin.py` | 既存の 2 点比較版(LEAD_MIN 世代別) |
| `docs/ev_strategy_findings.md` | 「drift は 3 ヶ月改善していない」節を追記済 |
| `logs/reflections/2026-08.md` | 発端の夜次反省ログ(2026-08-07) |
| `scripts/odds_timeseries_collector.py` | odds_ts の収集側。7 時点の定義はここ |
| `data/odds_ts/*.jsonl` | 入力データ(.gitignore、家 PC のみ) |

---

## 7. 出先 PC 側の状況(参考)

- Streamlit は `AutoraceStreamlitApp`(タスクスケジューラ)経由で常駐、HTTP 200 で稼働中
- メモリ逼迫は Firefox 1.4GB / claude 1.0GB / comet 0.8GB / Codex 0.5GB が主因。
  streamlit は 20MB で無実
- 未判断のまま残っている提案: **5 分ごとの streamlit watchdog タスク**追加
