requests>=2.31
python-dotenv>=1.0

# Analysis / ML
pandas>=2.2
numpy>=1.26
pyarrow>=15.0
scikit-learn>=1.4
lightgbm>=4.0
tabulate>=0.9

# Live prediction app
streamlit>=1.30
streamlit-autorefresh>=1.0

# Vote history (browser cookie source)
browser_cookie3>=0.20

# Auto-login (Playwright で vote.autorace.jp に自動ログイン、2026-05-08 導入)
# 初回は `playwright install chromium` も必要
playwright>=1.40
